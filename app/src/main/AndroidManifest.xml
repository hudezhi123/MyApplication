<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.test.hudezhi.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Light.NoTitleBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.LoginActivity" />
        <activity android:name=".ui.activity.WelcomeActivity" />
        <activity android:name=".ui.activity.AddRecordActivity" />
        <activity android:name=".ui.activity.IncomeActivity" />
        <activity android:name=".ui.activity.ExpendActivity" />
        <activity android:name=".ui.activity.RecordInfoActivity"></activity>
    </application>

</manifest>